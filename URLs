from django.urls import path, include

urlpatterns = [
    path('api/auth/', include('accounts.urls')),
    path('api/mental/', include('mental_health.urls')),
    path('api/therapists/', include('therapists.urls')),
]
from django.urls import path
from .views import login_view
from .serializers import RegisterSerializer
from rest_framework.decorators import api_view
from rest_framework.response import Response

@api_view(['POST'])
def register(request):
    serializer = RegisterSerializer(data=request.data)
    if serializer.is_valid():
        serializer.save()
        return Response({"message": "User created"})
    return Response(serializer.errors)

urlpatterns = [
    path('login/', login_view),
    path('register/', register),
]
from django.urls import path
from .views import create_assessment, daily_checkin

urlpatterns = [
    path('assessment/', create_assessment),
    path('checkin/', daily_checkin),
]
from django.urls import path
from .views import therapist_list

urlpatterns = [
    path('', therapist_list),
]
